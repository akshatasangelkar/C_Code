#include <stdio.h>

int main()
{
  int num,fact=1;
  printf("Enter the number ");
  scanf("%d",&num);
  if(num<1)
  {
      printf("Number is 0 or negative");
  }
  else
  {
      while(num>1)
      {
          fact*=num;
          num --;
      }
      printf("Factorial is %d ",fact);
  }
 }
