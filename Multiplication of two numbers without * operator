#include<stdio.h>

int main()
{
    int num1,num2,sum=0,i;
    printf("Enter the two numbers to multiply");
    scanf("%d  %d",&num1,&num2);
    for(i=1;i<=num1;i++)
    {
        sum=sum+num2;
    }
    printf("Multiplication of %d * %d is %d", num1,num2,sum);
}
